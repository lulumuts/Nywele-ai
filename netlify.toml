# Netlify configuration for Next.js
[build]
  command = "npm run build"
  publish = ".next"

# Next.js runtime plugin (auto-detects Next.js and handles routing)
[[plugins]]
  package = "@netlify/plugin-nextjs"
