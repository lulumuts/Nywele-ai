/** Page shell background — keep in sync with `:root --background` in `app/globals.css`. */
export const APP_PAGE_BACKGROUND = '#FFFEE1' as const;

/** Dashboard routine / product / health snapshot tiles and chart copy. */
export const DASHBOARD_CARD_TEXT = '#573203' as const;
